# -----------------------------------------------------------------------------
# Global application configuration
# -----------------------------------------------------------------------------
DJANGO_SETTINGS_MODULE=dokoplatform.settings.dev
DJANGO_DEBUG=False
DJANGO_SECRET_KEY=change-me
DJANGO_ALLOWED_HOSTS=example.com,.example.org
CSRF_TRUSTED_ORIGINS=https://example.com
CORS_ALLOWED_ORIGINS=https://frontend.example.com

# -----------------------------------------------------------------------------
# Database configuration (PostgreSQL is strongly recommended for production)
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://doko:strong-password@localhost:5432/doko
DJANGO_DB_ENGINE=django.db.backends.postgresql
DJANGO_DB_NAME=doko
DJANGO_DB_USER=doko
DJANGO_DB_PASSWORD=strong-password
DJANGO_DB_HOST=localhost
DJANGO_DB_PORT=5432
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=

# -----------------------------------------------------------------------------
# Optional services
# -----------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379/0
EMAIL_URL=

# -----------------------------------------------------------------------------
# Frontend configuration
# -----------------------------------------------------------------------------
NEXT_PUBLIC_API_BASE_URL=https://api.example.com
