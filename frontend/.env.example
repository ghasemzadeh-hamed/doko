# Public values exposed to the browser.
# Update NEXT_PUBLIC_API_BASE_URL to point at the deployed backend API.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# Uncomment and adjust if you proxy assets via a CDN.
# NEXT_PUBLIC_ASSETS_BASE_URL=https://cdn.example.com
